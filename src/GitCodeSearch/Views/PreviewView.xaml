<UserControl x:Class="GitCodeSearch.Views.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitCodeSearch.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox x:Name="ContentTextBox" IsReadOnly="True" FontFamily="Consolas" BorderThickness="0" Text="{Binding Content, Mode=OneWay}" 
                 HorizontalScrollBarVisibility="Auto" IsInactiveSelectionHighlightEnabled="True" 
                 VerticalScrollBarVisibility="Visible">
        </TextBox>
    </Grid>
</UserControl>
